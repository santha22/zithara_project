import React from 'react';

function Table({ data, setSortedBy }) {
  // Implement the table rendering logic here

  return (
    <table>
      {/* Table headers and rows */}
    </table>
  );
}

export default Table;
